---
title: "VFM-Loc: Zero-Shot Cross-View Geo-Localization via Aligning Discriminative Visual Hierarchies"
title_zh: VFM-Loc：通过对齐判别性视觉层级实现零样本跨视图地理定位
authors: "Jun Lu, Zehao Sang, Haoqi Wei, Xiangyun Liu, Kun Zhu, Haitao Guo, Zhihui Gong, Lei Ding"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13855v1"
tags: ["query:vlm-osm"]
score: 8.0
evidence: 利用视觉基础模型进行零样本跨视图地理定位
tldr: "针对跨视图地理定位（CVGL）中监督学习方法泛化性差的问题，本文提出VFM-Loc，一种无需训练的零样本框架。该方法利用视觉基础模型（VFM）的通用表征，通过分层线索提取机制捕捉多尺度判别性特征，并结合统计流形对齐技术在共享度量空间中对齐异构特征分布。实验表明，VFM-Loc在标准基准上表现优异，在极具挑战性的大倾角LO-UCV数据集上Recall@1超过监督方法20%以上，为现实世界CVGL提供了稳健的无训练范式。"
selection_source: fresh_fetch
motivation: 现有的监督学习方法在面对剧烈的视角差异和数据集偏差时，难以泛化到不受限的真实地理定位场景。
method: 提出一种结合分层视觉线索提取与统计流形对齐的无训练框架，利用预训练视觉基础模型实现跨视图特征匹配。
result: "在多个基准测试中表现强劲，特别是在大倾角LO-UCV数据集上，其Recall@1指标比监督方法提升了20%以上。"
conclusion: 通过对预训练特征进行原则性对齐，可以有效弥合跨视图差距，建立起一种稳健且无需训练的地理定位新范式。
---

## 摘要
遥感中的跨视图地理定位（CVGL）旨在通过将无人机视角查询与带有地理标记的卫星图像进行匹配来实现定位。尽管监督学习方法在闭集基准测试中取得了显著成果，但由于严重的视角差异和数据集偏差，它们往往难以泛化到不受约束的现实场景。为了克服这些局限性，我们提出了 VFM-Loc，这是一个用于零样本 CVGL 的免训练框架，它利用了视觉基础模型（VFMs）中具有泛化能力的视觉表示。VFM-Loc 通过渐进式对齐策略识别并匹配不同视角下的判别性视觉线索。首先，我们设计了一种层级线索提取机制，利用广义均值池化（Generalized Mean pooling）和尺度加权 RMAC（Scale-Weighted RMAC）在保持层级置信度的同时，保留跨尺度的独特视觉线索。其次，我们引入了一种基于领域级主成分分析（PCA）和正交普鲁克分析（Orthogonal Procrustes analysis）的统计流形对齐流水线，在共享度量空间中线性对齐异构特征分布。实验表明，VFM-Loc 在标准基准测试中表现出强大的零样本准确率，并且在具有大倾斜角的挑战性 LO-UCV 数据集上，其 Recall@1 超过监督方法 20% 以上。这项工作强调，对预训练特征进行有原则的对齐可以有效弥合跨视图差距，为现实世界的 CVGL 建立了一种鲁棒且免训练的范式。相关代码已发布于：https://github.com/DingLei14/VFM-Loc。

## Abstract
Cross-View Geo-Localization (CVGL) in remote sensing aims to locate a drone-view query by matching it to geo-tagged satellite images. Although supervised methods have achieved strong results on closeset benchmarks, they often fail to generalize to unconstrained, real-world scenarios due to severe viewpoint differences and dataset bias. To overcome these limitations, we present VFM-Loc, a training-free framework for zero-shot CVGL that leverages the generalizable visual representations from vision foundational models (VFMs). VFM-Loc identifies and matches discriminative visual clues across different viewpoints through a progressive alignment strategy. First, we design a hierarchical clue extraction mechanism using Generalized Mean pooling and Scale-Weighted RMAC to preserve distinctive visual clues across scales while maintaining hierarchical confidence. Second, we introduce a statistical manifold alignment pipeline based on domain-wise PCA and Orthogonal Procrustes analysis, linearly aligning heterogeneous feature distributions in a shared metric space. Experiments demonstrate that VFM-Loc exhibits strong zero-shot accuracy on standard benchmarks and surpasses supervised methods by over 20% in Recall@1 on the challenging LO-UCV dataset with large oblique angles. This work highlights that principled alignment of pre-trained features can effectively bridge the cross-view gap, establishing a robust and training-free paradigm for real-world CVGL. The relevant code is made available at: https://github.com/DingLei14/VFM-Loc.

---

## 论文详细总结（自动生成）

### 论文总结：VFM-Loc —— 基于判别性视觉层级对齐的零样本跨视图地理定位

#### 1. 核心问题与研究动机
*   **核心问题**：传统的跨视图地理定位（CVGL）方法高度依赖有监督学习，导致模型在面对未见过的地理区域、剧烈的视角变化（如无人机大倾角拍摄）或不同传感器数据时，泛化能力极差。
*   **研究动机**：现有的视觉基础模型（VFM）虽具备强大的通用表征能力，但由于无人机视角（斜视）与卫星视角（俯视）之间存在严重的几何畸变和特征分布偏移（Domain Shift），直接“开箱即用”效果不佳。本文旨在提出一种**无需训练（Training-free）**的框架，通过对齐特征流形来弥合视角鸿沟。

#### 2. 方法论
VFM-Loc 采用三阶段的流水线处理，核心思想是利用预训练 VFM 的语义知识，通过统计学方法进行特征对齐：
*   **分层判别性线索提取**：
    *   **GeM 池化**：使用广义均值池化（Generalized Mean pooling）替代传统的平均池化，通过可调参数 $p$ 强化显著性地标（如建筑边缘、交叉口）的特征，抑制背景噪声。
    *   **尺度加权 R-MAC**：引入区域最大激活卷积（R-MAC）策略，将特征图划分为多尺度网格，并利用衰减因子 $\alpha$ 平衡全局上下文与局部细节，增强对局部匹配的鲁棒性。
*   **统计流形对齐（SMA）**：
    *   **领域级 PCA 预处理**：分别对无人机和卫星特征进行主成分分析，通过零中心化消除平移偏差，并保留高方差维度以去噪。
    *   **正交普鲁克分析（Orthogonal Procrustes）**：通过奇异值分解（SVD）求解最优正交变换矩阵 $R$，在保持特征拓扑结构不变的前提下，将无人机特征流形旋转对齐至卫星特征空间。

#### 3. 实验设计
*   **数据集**：
    1.  **University-1652**：标准多视图基准数据集。
    2.  **SUES-200**：包含四个不同飞行高度的城市场景数据集。
    3.  **LO-UCV（新提出）**：大倾角无人机-卫星数据集，视角倾斜超过 45°，场景涵盖山地、城市、农田等，极具挑战性。
*   **对比方法**：
    *   **有监督方法**：Sample4Geo, DAC, QDFL 等。
    *   **无监督方法**：EM-CVGL, UniABG 等。
*   **评价指标**：Recall@1 (R@1) 和平均精度（AP）。

#### 4. 资源与算力
*   **训练算力**：由于该框架是**免训练（Training-free）**的，因此**不需要任何训练算力或 GPU 训练时长**。
*   **推理成本**：文中对比了不同 VFM 后端的推理开销。以 DINOv3 为例，参数量为 85.67M，计算量为 12.93 GFLOPs。相比于需要复杂适配器的无监督方法，其推理过程更为轻量。

#### 5. 实验数量与充分性
*   **实验规模**：作者在三个主流数据集上进行了详尽的对比实验，并针对组件有效性、池化策略、R-MAC 衰减因子 $\alpha$、不同 VFM 后端（DINOv3, SAM3, RadioV4）进行了消融研究。
*   **充分性与公平性**：实验涵盖了从标准场景到极端大倾角场景的迁移，特别是**跨数据集泛化实验**（在 A 数据集训练/对齐，在 B 数据集测试），证明了该方法在未见场景下的优越性。对比实验均采用了相同的评估协议，结果客观。

#### 6. 主要结论与发现
*   **对齐是关键**：直接使用 VFM 特征效果很差（R@1 仅约 21%），但经过 SMA 流形对齐后，性能大幅提升（提升超 50%），证明跨视图障碍主要是统计分布失配而非语义缺失。
*   **零样本超越有监督**：在极具挑战性的 LO-UCV 数据集上，VFM-Loc 的 R@1 达到 89.84%，比最强的有监督方法高出 **20%~30%**。
*   **高度稳定性**：在 SUES-200 的不同高度测试中，VFM-Loc 均保持了 99% 以上的极高准确率，表现出极强的环境适应性。

#### 7. 优点（亮点）
*   **完全免训练**：无需昂贵的标注数据和反向传播过程，极大地降低了部署门槛。
*   **即插即用**：能够直接适配各种先进的视觉基础模型（如 DINOv3, Radio）。
*   **鲁棒性强**：通过正交变换对齐流形，既解决了视角差异问题，又保留了原始特征的判别力。
*   **泛化性极佳**：在真实世界的大倾角、多地貌场景下表现远超现有的监督学习模型。

#### 8. 不足与局限
*   **依赖基础模型质量**：其性能上限受限于所选 VFM 的预训练质量，若 VFM 在特定遥感地物上的表征能力不足，对齐效果也会受限。
*   **线性对齐限制**：正交普鲁克分析本质上是一种线性变换，对于极其复杂的非线性几何畸变，可能仍存在对齐残差。
*   **实时性挑战**：虽然免训练，但若使用超大规模 VFM（如 RadioV4-Huge），在嵌入式无人机平台上的实时推理可能存在压力。

（完）
