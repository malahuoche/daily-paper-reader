---
title: "LoD-Loc v3: Generalized Aerial Localization in Dense Cities using Instance Silhouette Alignment"
title_zh: LoD-Loc v3：基于实例轮廓对齐的稠密城市通用航空定位
authors: "Shuaibang Peng, Juelin Zhu, Xia Li, Kun Yang, Maojun Zhang, Yu Liu, Shen Yan"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19609v1"
tags: ["query:vlm-osm"]
score: 8.0
evidence: 通过语义建筑轮廓对齐进行航空定位
tldr: 本研究针对密集城市环境中的航空视觉定位问题，提出了 LoD-Loc v3。针对前代方法在跨场景泛化及密集建筑场景中表现不佳的局限，该方法引入了两大创新：一是构建了包含10万张图像的大规模实例分割合成数据集 InsLoD-Loc，显著提升了模型的零样本泛化能力；二是将定位范式从语义轮廓对齐转变为实例轮廓对齐，有效降低了密集场景下的位姿估计歧义。实验证明，该方法在多种复杂场景下均达到了 SOTA 性能。
selection_source: fresh_fetch
motivation: 前代方法 LoD-Loc v2 在跨场景泛化能力和密集建筑场景下的定位精度方面存在明显局限。
method: 通过构建大规模合成数据集 InsLoD-Loc 并将定位范式从语义对齐转向实例轮廓对齐，提升了模型的泛化性与鲁棒性。
result: LoD-Loc v3 在跨场景和密集城市场景的定位任务中均大幅领先于现有的最先进基准方法。
conclusion: 实例轮廓对齐结合大规模合成数据训练，是解决复杂城市环境下航空定位歧义和泛化难题的有效途径。
---

## 摘要
我们提出了 LoD-Loc v3，这是一种用于稠密城市环境通用航空视觉定位的新方法。尽管先前的工作 LoD-Loc v2 通过将语义建筑轮廓与低细节城市模型对齐来实现定位，但它面临两个关键局限：跨场景泛化能力差，以及在建筑稠密场景中经常失效。我们的方法通过两项关键创新解决了这些挑战。首先，我们开发了一种新的合成数据生成流水线，产生了 InsLoD-Loc——这是迄今为止最大的航空图像实例分割数据集，包含 10 万张具有精确建筑实例标注的图像。这使得训练后的模型展现出卓越的零样本泛化能力。其次，我们重新构建了定位范式，从语义轮廓对齐转向实例轮廓对齐，这显著降低了稠密场景中的位姿估计歧义。大量实验表明，LoD-Loc v3 优于现有的最先进（SOTA）基准，在跨场景和稠密城市场景中均以大幅领先的优势实现了卓越性能。项目地址：https://nudt-sawlab.github.io/LoD-Locv3/。

## Abstract
We present LoD-Loc v3, a novel method for generalized aerial visual localization in dense urban environments. While prior work LoD-Loc v2 achieves localization through semantic building silhouette alignment with low-detail city models, it suffers from two key limitations: poor cross-scene generalization and frequent failure in dense building scenes. Our method addresses these challenges through two key innovations. First, we develop a new synthetic data generation pipeline that produces InsLoD-Loc - the largest instance segmentation dataset for aerial imagery to date, comprising 100k images with precise instance building annotations. This enables trained models to exhibit remarkable zero-shot generalization capability. Second, we reformulate the localization paradigm by shifting from semantic to instance silhouette alignment, which significantly reduces pose estimation ambiguity in dense scenes. Extensive experiments demonstrate that LoD-Loc v3 outperforms existing state-of-the-art (SOTA) baselines, achieving superior performance in both cross-scene and dense urban scenarios with a large margin. The project is available at https://nudt-sawlab.github.io/LoD-Locv3/.

---

## 论文详细总结（自动生成）

以下是对论文《LoD-Loc v3: Generalized Aerial Localization in Dense Cities using Instance Silhouette Alignment》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：无人机（UAV）在稠密城市环境中的视觉定位。
*   **研究背景**：传统的视觉定位依赖高精度 3D 重建（如 SfM），但成本高且存在隐私风险。基于城市细节水平（LoD）模型的定位方法（如 LoD-Loc v2）通过对齐建筑轮廓提供了替代方案。
*   **动机/痛点**：
    *   **泛化性差**：前代方法在未见过的场景中表现不佳，缺乏跨区域的零样本（Zero-shot）能力。
    *   **稠密场景歧义**：在建筑密集的区域，多个建筑的语义轮廓会融合成一个巨大的“色块”，导致几何约束丢失，位姿估计失效。

### 2. 提出的方法论
LoD-Loc v3 的核心思想是将定位范式从**语义轮廓对齐**转变为**实例轮廓对齐**。

*   **大规模合成数据流水线**：
    *   利用 Unreal Engine 5 (UE5) 和 Google Earth 3D Tiles 生成逼真的 RGB 图像。
    *   通过 OpenSceneGraph (OSG) 将 LoD 模型与 3D Tiles 坐标对齐，自动生成精确的实例分割标签。
    *   构建了 **InsLoD-Loc** 数据集：包含 10 万张图像，覆盖 6 个国家的 40 个区域。
*   **建筑实例分割模型**：
    *   基于 SAM（Segment Anything Model）进行微调。
    *   引入可学习的 **Prompter Module** 预测提示嵌入，并使用 **LoRA** 技术对 SAM 的视觉编码器进行参数高效微调。
*   **实例轮廓对齐算法**：
    *   **模型实例化**：通过拓扑结构分析（图划分）为 LoD 模型中的每个建筑分配唯一 ID。
    *   **代价函数（Cost Function）**：计算查询图像预测实例与渲染实例之间的 Dice 系数。
    *   **权重策略**：提出基于置信度（Confidence-based）和基于面积（Area-based）的加权对齐策略，以优化粗到精（Coarse-to-Fine）的定位过程。

### 3. 实验设计
*   **数据集**：
    *   **UAVD4L-LoDv2**：2.5 $km^2$ 的公共基准。
    *   **Swiss-EPFLv2**：8.2 $km^2$ 的公共基准。
    *   **Tokyo-LoDv3**（本文构建）：包含 5 个极具挑战性的稠密城市场景。
*   **对比基准（Baselines）**：
    *   **特征匹配类**：CAD-Loc（结合 SIFT, SuperPoint+SuperGlue, LoFTR, RoMa 等）。
    *   **特征对齐类**：MC-Loc（结合 DINOv2, RoMa）。
    *   **LoD 对齐类**：LoD-Loc (v1) 和 LoD-Loc v2。
*   **评价指标**：在 (2m, 2°), (3m, 3°), (5m, 5°) 阈值下的定位成功率，以及中值平移/旋转误差。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA RTX 4090 GPU**。
*   **训练细节**：
    *   在 InsLoD-Loc 的 8.8 万张训练集上训练了 20 个 Epoch。
    *   输入图像尺寸为 512x512。
    *   使用了 AdamW 优化器，基础学习率为 $2 \times 10^{-4}$。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个大型数据集上进行了详尽测试，涵盖了从郊区到极稠密中心城区的多种环境。
*   **消融实验**：
    *   验证了**轮廓表示**的影响（语义 vs 实例）：证明即使在相同数据集下训练，实例表示也远优于语义表示。
    *   验证了**实例对齐机制**的有效性：通过强制合并实例掩码进行对比，证明了独立实例约束的重要性。
*   **客观性**：实验采用了跨场景（Cross-scene）测试，即在合成数据上训练，在真实/不同城市的测试集上评估，充分验证了模型的零样本泛化能力，实验设计公平且具有说服力。

### 6. 主要结论与发现
*   **性能飞跃**：在稠密场景（Tokyo-LoDv3）中，LoD-Loc v3 在 (2m, 2°) 精度下的表现比现有 SOTA 方法提升了 **2000%**。
*   **泛化能力**：仅通过合成数据训练，模型即可在真实世界的不同城市（如瑞士、日本）实现高精度定位，无需针对特定场景微调。
*   **解决歧义**：实例级别的对齐提供了更细粒度的几何约束，有效解决了建筑群融合导致的定位漂移问题。

### 7. 优点
*   **范式创新**：首次将实例分割引入 LoD 定位，从根本上解决了稠密城市的歧义问题。
*   **数据贡献**：发布了目前最大的航空建筑实例分割数据集 InsLoD-Loc，对社区具有极高价值。
*   **实用性强**：不依赖昂贵的点云地图，仅需低细节的 LoD1 模型即可实现米级定位。

### 8. 不足与局限
*   **分割依赖性**：定位精度高度依赖于实例分割的质量。在极端恶劣天气（如暴雨、浓雾）或光照极差的情况下，分割模型可能失效，进而导致定位失败。
*   **计算开销**：虽然使用了 LoRA，但基于 SAM 的推理在嵌入式 UAV 平台上可能仍存在实时性挑战。
*   **地图一致性**：如果 LoD 模型与现实场景存在巨大的年代差异（如大面积拆迁或新建），实例对齐的鲁棒性可能会受到影响。

（完）
