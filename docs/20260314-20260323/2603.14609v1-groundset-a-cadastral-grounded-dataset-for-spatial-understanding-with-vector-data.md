---
title: "GroundSet: A Cadastral-Grounded Dataset for Spatial Understanding with Vector Data"
title_zh: GroundSet：一种基于地籍矢量数据的空间理解数据集
authors: "Roger Ferrod, Maël Lecene, Krishna Sapkota, George Leifman, Vered Silverman, Genady Beryozkin, Sylvain Lobry"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14609v1"
tags: ["query:vlm-osm"]
score: 8.0
evidence: 利用矢量数据和遥感进行空间理解
tldr: 针对多模态大模型在遥感领域细粒度空间理解能力的不足，本文推出了GroundSet数据集。该数据集基于可验证的地籍矢量数据，包含51万张高分辨率图像、380万个标注对象及135个语义类别。通过在七项空间推理任务上的指令微调实验，证明了高质量监督数据能显著提升标准模型架构的细粒度空间定位与理解能力，填补了遥感空间推理数据的空白。
selection_source: fresh_fetch
motivation: 现有的遥感多模态大模型因依赖有限或陈旧的数据集，在处理细粒度空间理解任务时存在明显的性能缺陷。
method: 构建了基于地籍矢量数据的大规模数据集GroundSet，并利用LLaVA架构在七个空间推理任务上进行指令微调以建立基准。
result: 实验证明，通过GroundSet的高保真监督，标准模型架构能够克服零样本学习的局限，在细粒度空间定位任务上表现优异。
conclusion: 高质量的领域特定标注数据是提升模型空间理解能力的关键，无需复杂的架构修改即可实现显著的性能突破。
---

## 摘要
地球观测中的精确空间理解对于将原始航空图像转化为城市规划、环境监测和灾害管理等关键应用的可行见解至关重要。然而，多模态大语言模型在遥感领域的细粒度空间理解方面存在严重缺陷，这主要是由于其依赖于有限或重新利用的遗留数据集。为了弥补这一差距，我们推出了一个基于可验证地籍矢量数据的大规模数据集，包含 51 万张高分辨率图像中的 380 万个标注对象，涵盖 135 个细粒度语义类别。我们通过一个涵盖七个空间推理任务的综合指令微调基准对该资源进行了验证。我们的评估使用标准的 LLaVA 架构建立了一个稳健的基准。研究表明，虽然目前的遥感专用模型和商业模型（如 Gemini）在零样本设置下表现挣扎，但高保真监督有效地弥补了这一差距，使标准架构无需复杂的架构修改即可掌握细粒度的空间定位。

## Abstract
Precise spatial understanding in Earth Observation is essential for translating raw aerial imagery into actionable insights for critical applications like urban planning, environmental monitoring and disaster management. However, Multimodal Large Language Models exhibit critical deficiencies in fine-grained spatial understanding within Remote Sensing, primarily due to a reliance on limited or repurposed legacy datasets. To bridge this gap, we introduce a large-scale dataset grounded in verifiable cadastral vector data, comprising 3.8 million annotated objects across 510k high-resolution images with 135 granular semantic categories. We validate this resource through a comprehensive instruction-tuning benchmark spanning seven spatial reasoning tasks. Our evaluation establishes a robust baseline using a standard LLaVA architecture. We show that while current RS-specialized and commercial models (e.g., Gemini) struggle in zero-shot settings, high-fidelity supervision effectively bridges this gap, enabling standard architectures to master fine-grained spatial grounding without complex architectural modifications.

---

## 论文详细总结（自动生成）

### 论文总结：GroundSet - 基于地籍矢量数据的遥感空间理解数据集

#### 1. 核心问题与整体含义（研究动机和背景）
当前多模态大语言模型（MLLM）在遥感（RS）领域的应用受阻，核心瓶颈在于**细粒度空间理解能力不足**（如精确的目标检测和分割）。现有的遥感数据集大多源自陈旧的闭集检测数据集（如 DOTA、DIOR），类别有限（通常仅几十类）且标注精度不足。本文提出了 **GroundSet**，这是一个利用官方地籍（Cadastral）矢量数据构建的大规模数据集，旨在通过高保真、可验证的地理空间数据，提升模型对复杂遥感场景的语义认知和空间定位能力。

#### 2. 论文提出的方法论
*   **核心思想**：放弃依赖众包或陈旧标注，转而利用国家测绘机构（如法国 IGN）提供的法律验证级地籍矢量数据作为真值（Ground Truth）。
*   **关键技术细节**：
    *   **数据源**：结合 20 厘米高分辨率航空正射影像与 1 米精度的矢量模型（多边形、线、点）。
    *   **处理流水线**：
        1.  **过滤与对齐**：剔除不可见的地下设施（如隧道），确保视觉特征与矢量数据严格对应。
        2.  **语义重采样**：针对农村地区冗余度高的问题，通过场景组合去重，将数据量缩减 85% 以平衡分布。
        3.  **几何合并**：将行政划分的多块建筑合并为视觉连续的实体。
        4.  **指令生成**：利用模板和 VLM（如 Gemma-3）将矢量数据转化为 7 类任务的指令（描述、分类、检测、多目标检测、指代理解 REC、分割、VQA）。
    *   **视觉-语义差异处理**：利用 Gemini-2.5 Flash 自动识别并标记约 3%-7% 的遮挡或缺失样本，提供“干净”和“困难”训练集。

#### 3. 实验设计
*   **数据集规模**：全集包含 51 万张图像、380 万个对象、135 个细粒度类别。微调子集包含 6 万张图像和 180 万条指令。
*   **Benchmark 任务**：涵盖场景描述、局部分类、水平框检测（HBB）、多类别检测、指代理解（REC）、多边形分割和存在性 VQA。
*   **对比方法**：
    *   **通用模型**：LLaVA-1.5/1.6、Gemini-2.5 Flash、PaliGemma-2、Ferret。
    *   **遥感专用模型**：GeoChat、SkySenseGPT、VRSBench 基准模型。
*   **跨域验证**：在 VRSBench 数据集上进行零样本（Zero-shot）迁移测试。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 (80GB) GPU**。
*   **训练细节**：采用 LoRA 效率微调技术，在 LLaVA-1.6-7B 基础上训练 1 个 epoch，总耗时约 **72 小时**。
*   **推理设置**：所有模型均在 4-bit 量化下运行以保证评估效率。

#### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的零样本对比实验，涵盖了当前主流的开源和商业模型。
*   **消融实验**：针对文本增强、分割增强、视觉编码器冻结与否、以及遮挡样本过滤等变量进行了 5 组消融实验。
*   **充分性评价**：实验设计较为充分。不仅对比了同架构模型以证明“数据胜过架构”，还通过跨数据集（VRSBench）测试验证了模型在处理未见类别（如飞机、车辆）时的空间定位迁移能力。

#### 6. 论文的主要结论与发现
*   **数据质量是关键**：使用标准 LLaVA 架构在 GroundSet 上训练后的表现，远超复杂的遥感专用架构和商业模型（如 Gemini 在分类上落后 44 个点）。
*   **知识鸿沟**：现有模型在细粒度类别（如遗产地、特定工业设施）上表现极差，证明了地籍数据引入了互联网预训练中缺失的领域知识。
*   **空间能力可迁移**：即使在未见过的目标（如移动车辆）上，经过 GroundSet 训练的模型依然展现出比遥感专用模型更强的定位鲁棒性。

#### 7. 优点
*   **高保真度**：利用法律验证的地籍数据，解决了遥感标注中常见的几何不精确和语义模糊问题。
*   **语义丰富性**：135 个类别远超现有数据集，涵盖了极细粒度的行政和工业分类。
*   **原生矢量支持**：直接提供多边形顶点坐标而非栅格掩码，更符合现代 MLLM 的序列化建模需求。
*   **开源贡献**：同时发布了原始矢量数据和指令微调数据集，具有很高的科研价值。

#### 8. 不足与局限
*   **地理偏差**：目前数据仅覆盖法国（20 个省），虽然方法论通用，但视觉特征可能存在地域局限性。
*   **目标局限性**：地籍数据主要涵盖**静态设施**（建筑、道路、土地利用），缺乏对移动目标（车辆、船只、飞机）的覆盖。
*   **视觉-语义不一致**：尽管有自动过滤，但由于拍摄时间差异，矢量图层与影像之间仍存在约 15% 的潜在不匹配风险。

（完）
